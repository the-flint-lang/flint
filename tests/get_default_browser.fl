fn get_brwoser_bin_path_and_exec(name: string) void {
    const path = exec("xdg-settings get default-web-browser") 
        ~> trim() 
        ~> split(".");
    
    const cmd = path[0] ~> concat(" ") ~> concat(name);
    exec(cmd);
}
