import process;
import str;

fn get_brwoser_bin_path_and_exec(name: string) void {
    const path = process.exec("xdg-settings get default-web-browser") 
        ~> str.trim() 
        ~> str.split(".");
    
    const cmd = $"{path[0]} {name}";
    process.exec(cmd);
}