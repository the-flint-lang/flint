import "std/io.fl" as io;
import "std/os.fl" as os;

_ = os.touch("tests/io/test_rm_only") catch |err| {
    print(err);
    os.exit(1);
};

_ = os.rm("tests/io/test_rm_only") catch |err| {
    print(err);
    os.exit(1);
};

print("ok");