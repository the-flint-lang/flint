import io;
import process;
import fs;

_ = fs.touch("tests/io/test_rm_only") catch |err| {
    print(err);
    process.exit(1);
};

_ = fs.rm("tests/io/test_rm_only") catch |err| {
    print(err);
    process.exit(1);
};

print("ok");