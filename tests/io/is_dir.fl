import io;
import fs;

fn rm_() void {
    _ = fs.rm_dir("test") catch |err| {
        print(err);
    };
}

_ = fs.mkdir("test") catch |err| {
    print(err);
    process.exit(1);
};

if fs.is_dir("test") {
    print("Is a dir");
    rm_();
    process.exit(0);
} 

print("Not is a dir");
rm_();