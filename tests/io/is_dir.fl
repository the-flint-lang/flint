import io;
import os;

fn rm_() void {
    _ = os.rm_dir("test") catch |err| {
        print(err);
    };
}

_ = os.mkdir("test") catch |err| {
    print(err);
    os.exit(1);
};

if os.is_dir("test") {
    print("Is a dir");
    rm_();
    os.exit(0);
} 

print("Not is a dir");
rm_();