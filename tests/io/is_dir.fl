_ = mkdir("test") catch |err| {
    print(err);
    exit(1);
};

if is_dir("test") {
    print("Is a dir");
    rm_();
    exit(0);
} 

print("Not is a dir");
rm_();

fn rm_() void {
    _ = rm_dir("test") catch |err| {
        print(err);
    };
}