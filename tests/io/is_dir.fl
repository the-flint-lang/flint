import io;
import os;

_ = os.mkdir("test") catch |err| {
    print(err);
    os.exit(1);
};

if os.is_dir("test") {
    print("Is a dir");
    rm_();
    os.exit(0);
} 

print("Not is a dir");
rm_();

fn rm_() void {
    _ = os.rm_dir("test") catch |err| {
        print(err);
    };
}