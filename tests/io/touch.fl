import io;
import fs;
import process;

_ = fs.touch("tests/io/teste.test") catch |err| {
    print(err);
    process.exit(1);
};

_ = fs.rm("tests/io/teste.test") catch |err| {
    print(err);
    process.exit(1);
};


print("ok");