import io;
import os;

_ = os.touch("tests/io/teste.test") catch |err| {
    print(err);
    os.exit(1);
};

_ = os.rm("tests/io/teste.test") catch |err| {
    print(err);
    os.exit(1);
};


print("ok");