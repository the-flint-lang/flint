import io;
import os;
import process;

_ = fs.mkdir("tests/io/test_mkdir") catch |err| {
    print(err);
    process.exit(1);
};

_ = fs.rm_dir("tests/io/test_mkdir") catch |err| {
    print(err);
    process.exit(1);
};

print("ok");