import io;
import fs;
import process;

_ = fs.mkdir("tests/io/test_rm_dir") catch |err| {
    print(err);
    process.exit(1);
};

_ = fs.rm_dir("tests/io/test_rm_dir") catch |err| {
    print(err);
    process.exit(1);
};


print("ok");