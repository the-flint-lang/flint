import io;
import os;

_ = os.mkdir("tests/io/test_rm_dir") catch |err| {
    print(err);
    os.exit(1);
};

_ = os.rm_dir("tests/io/test_rm_dir") catch |err| {
    print(err);
    os.exit(1);
};


print("ok");