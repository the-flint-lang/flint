import "std/io.fl" as io;
import "std/os.fl" as os;

_ = os.copy("tests/io/copy.fl", "tests/io/copy_temp.out") catch |err| {
    print(err);
    os.exit(1);
};

_ = os.rm("tests/io/copy_temp.out") catch |err| {
    print(err);
    os.exit(1);
};