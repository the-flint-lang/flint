import io;
import fs;
import process;

_ = fs.copy("tests/io/copy.fl", "tests/io/copy_temp.out") catch |err| {
    print(err);
    process.exit(1);
};

_ = fs.rm("tests/io/copy_temp.out") catch |err| {
    print(err);
    process.exit(1);
};