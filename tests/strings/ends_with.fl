import str;
import fs;

for file in fs.ls(".") ~> lines() {
    if (str.ends_with(to_str(file), ".fl")){
        print($"{file} is a flint source code.");
    }
    
    if (str.ends_with(to_str(file), ".c")){
        print($"{file} is a c source code.");
    }
}