import "std/io.fl" as io;
import "std/strings.fl" as str;

var numbers = [10, 20, 30, 40];
print("Before mutation:");
print(str.int_to_str(numbers[2]));

numbers[2] = 99;
print("After mutation:");
print(str.int_to_str(numbers[2]));