import "std/io.fl" as io;
import "std/strings.fl" as str;

const numbers = [10, 20, 30, 40];
const servers = ["aws", "gcp", "azure"];

for n in numbers {
    print(str.int_to_str(n));
}

for s in servers {
    print(s);
}