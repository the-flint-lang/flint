import "std/io.fl" as io;
import "std/os.fl" as os;

fn check_arguments() void {
    const my_args = os.args();
    
    print("Test 2: Listing passed arguments...");
    
    for argument in my_args {
        print(argument);
    }
}

check_arguments();