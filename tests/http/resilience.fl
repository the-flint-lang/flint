import http;
import process;

const body = http.fetch("https://url-que-nao-existe-flint-test.xyz") catch |err| {
    print("fetch failed as expected");
    print(err);
    process.exit(0);
};
print(body);
