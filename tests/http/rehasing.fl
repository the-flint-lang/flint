print("Starting Memory Stress Test (Rehashing)...");

const my_json = parse_json("{}");

print("Injecting 1,000 keys into the dictionary...");

for i in range(0, 1000) {
    # 1. Converts pure Integer to String
    const num_str = int_to_str(i);
    
    # 2. Concatenate natively
    const final_key = concat("key_", num_str);
    
    # 3. Inject into the Dictionary and force the Load Limit!
    set(my_json, final_key, i);
}

print("Proven expansion capacity!");
print("Reading key_999:");
print(get(my_json, "key_999"));
