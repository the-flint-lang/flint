import "std/io.fl" as io;
import "std/strings.fl" as str;
import "std/os.fl" as os;

(io.read_file("bench_data.log") catch |err| { print(err); os.exit(1); }) 
    ~> str.to_str()
    ~> str.count_matches("ERROR")
    ~> print();
