import http;
import json;
import strings;
import os;

const payload = http.fetch("https://dummyjson.com/products/1") catch |e| { os.exit(1); };
const data = json.parse(strings.to_str(payload));
print(strings.to_str(get(data, "title")));
