import "std/http.fl" as http;
import "std/json.fl" as json;
import "std/strings.fl" as str;
import "std/os.fl" as os;

const payload = http.fetch("https://dummyjson.com/products/1") catch |e| { os.exit(1); };
const data = json.parse(str.to_str(payload));
print(str.to_str(get(data, "title")));
