import io;
import str;

struct Config {
    target_id: int,
    target_name: string,
    target_active: bool
}

const payload = io.read_file("typed_data.json");
const cfg = parse_json_as(Config, str.to_str(payload));
print(cfg.target_name);
